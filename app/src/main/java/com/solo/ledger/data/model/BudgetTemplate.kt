package com.solo.ledger.data.model

enum class BudgetTemplate(val displayName: String) {
    Student("Student Budget"),
    Hostel("Hostel Budget"),
    Saver("Saver Budget"),
    Minimal("Minimal Budget"),
}
